source ./init-env-vars

RANDO=randomxyz

curl --insecure -v -X POST \
  -H 'Content-Type: application/json' \
  -H "Authorization: bearer "$AUTH_TOKEN \
  -d "{ \"access_token\": \"$RANDO\" }" \
  $UPLOAD_URL